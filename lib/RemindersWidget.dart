import 'package:flutter/material.dart';

class RemindersWidget extends StatelessWidget {
  const RemindersWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Note: The list of reminders can use the /Reminders directory (set up similar to how Tasks are set up
    return const Text('Hello, reminders!');
  }
}
