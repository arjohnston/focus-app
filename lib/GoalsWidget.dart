import 'package:flutter/material.dart';

class GoalsWidget extends StatelessWidget {
  const GoalsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hello, goals!');
  }
}
